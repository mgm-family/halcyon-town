---
description: Shorthand for /reveal — bring the staged section tagged 【N】 in the Google Doc live on the site.
---

# /r — shorthand for /reveal

This is a plain alias. Take the numeric argument given here (e.g. `/r 5` means N=5) and follow `.claude/commands/reveal.md` exactly, using that number as the `/reveal` argument — same staging convention (`【N】` tags on draft headings), same steps, same PR-not-auto-merge default.
