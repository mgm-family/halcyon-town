# Vulfayne City（VAY City）ルールサイト ― 運用原則

このリポジトリは FiveM QBCore サーバー「Vulfayne City（VAY city）」の
**規程・ロア公開サイト**（静的1ページ、ビルド不要）。
Lua スクリプト本体（resources/ 配下）は別リポジトリで管理しており、
そちらの作業では `vay-city-dev` スキルを必ず確認すること。このリポジトリは対象外。

## 構成

- `index.html` … 単一ファイル。サイドバーのタブ切り替えで各パネルを表示するSPA。
  - 122行目付近に巨大な base64 埋め込み画像（ロゴ）があるため、
    `Read` で全体を読もうとせず `sed -n` 等で範囲指定して読むこと。
- `data/*.json` … 各タブの本文データ。編集して push すればそのままサイトに反映される。
  - **flat形式**（`rules.json` / `entry.json` / `compensation.json`）:
    `{ blocks: [{ num, title, html }, ...] }`
  - **accordion形式**（`civil.json` / `civilian.json` / `crime.json`）:
    `{ accent, groups: [{ tag, title, open, html }, ...] }`
  - `html` フィールドは生HTML文字列として `innerHTML` に挿入される。タグの閉じ忘れに注意。

## デザイン・トンマナ（毎回指示されなくても維持するルール）

`index.html` の `:root` に定義済みのトークンを必ず使う。新しい色を勝手に増やさない。

```
--ink:#0a0908        本文背景（最暗）
--ink-2:#141109       サイドバー等の背景グラデ
--ink-3:#1c1810       パネル背景
--line:#3a2f1e        枠線
--line-soft:#2a2417   淡い枠線
--gold:#c9a227        アクセント（基本）
--gold-bright:#e6c352 アクセント（強調・リンク）
--gold-dim:#7a641c    アクセント（沈める）
--vermillion:#ad3a2b       警告・犯罪系アクセント
--vermillion-bright:#d5543f
--blue:#4b6a8a         公務員系アクセント
--blue-bright:#6f93b6
--paper:#eae2cd        本文文字色
--paper-dim:#b7ad92    サブテキスト
--muted:#8c8368 / --muted-2:#5f5a48  ラベル・キャプション
```

- 見出し（h1〜h4）: `Shippori Mincho`（明朝、和風・格式）
- 本文: `Noto Sans JP`
- 世界観は「双つの月が照らす街」＝和風×神秘×格式のトーン。カジュアルな絵文字や
  ポップな配色は世界観を壊すため使わない。
- 新しいセクション/タブを追加する場合は既存の `.block` / `.panel` / accordion 構造を
  踏襲し、`accent`（civil=blue系, crime=vermillion系 等）の使い分けに倣う。

## 対応スタイル（3つのスキル原則）

### 1. 日本語入出力の最適化
- 音声入力・メモ書き・崩れた指示文はそのまま処理せず、意図を汲んで自然な日本語に
  整理してから作業に着手する。誤字脱字・語順の乱れは黙って正しく解釈する。
- 説明は「結論→理由→具体的な数字/手順」の順。初心者向けに専門用語は避けるか
  一言で補足する。
- エラー報告を受けたときは、原因（タイポ／Client-Server混同／nil参照など）と
  直し方を順を追って説明する（このリポジトリでは主にJSON構文エラーやHTML閉じタグ漏れが該当）。

### 2. メモ・学習・フィードバックの蓄積
- 作業中に得た重要な知見（「この書き方は避ける」「このJSONキー名の綴りに注意」等）は
  下の「学びとフィードバック」セクションに追記していく。
- 次回以降の作業では、このセクションを必ず読んでから着手し、過去の指摘を繰り返さない。
- Obsidian Vaultにアクセスできるローカルセッションで作業する場合は、下記「Obsidian連携」の
  手順に従い、このセクションの内容をVault側にも同期する。

### 3. デザイン・トンマナの標準化
- 上記のカラートークン／フォント／構造を毎回の指示なしに適用する。
- 新規パネルや装飾を追加する際も、既存のグラデーション・アニメーション（ambientPulse,
  twinkle 等）のトーンを踏襲し、独自の突飛な演出は追加しない。

---

## Obsidian連携

- このリポジトリはクラウド上のセッション（GitHubリポジトリのみアクセス可）からも
  操作されるため、ここには直接 Obsidian Vault へ書き込めない。
- ローカルのClaude Codeセッション（`vay-city-dev` 運用でObsidian Vaultにアクセスできる環境）で
  このリポジトリを開いた場合は、以下を行うこと。
  1. Vault内に **「Vulfayne City ルールサイト運用メモ.md」** という専用ノートを探す。
     存在しなければ新規作成してよい。
  2. 下の「学びとフィードバック」セクションとそのノートの内容を突き合わせ、
     差分（このファイルにだけある新しい行）があればノート側に転記する。
  3. 転記は要約でよいが、日付・対象ファイル（例: `crime.json`）が分かる形を保つ。
  4. 転記済みの行をこのファイルから削除する必要はない（このファイルはリポジトリ内の
     一次情報として残す。Obsidian側はその集約・横断参照用）。

## 学びとフィードバック（随時追記）

<!-- 例:
- 2026-XX-XX: crime.json の accent は必ず vermillion 系を使うこと（blueにすると civil と混同する）
- 2026-XX-XX: block.html に <script> を書いても実行されない（innerHTML経由のため）
-->

（まだ記録はありません。作業を重ねる中でここに追記していきます）
